/* 
* this file handle the html request 
*/

const handleHtmlRequest = async request => {
    // let get the url 
    //console.log(request.headers());
    //console.log(request.url());
    //console.log();
}

export default handleHtmlRequest;
